%% polinetwork-commands.sty
%% PoliNetwork APS — Custom Commands
%% Placeholder commands for logo, signature, dates, association data
%%
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{polinetwork-commands}[2026/04/01 PoliNetwork Custom Commands]

\RequirePackage{graphicx}
\RequirePackage{xcolor}
\RequirePackage{tabularx}
\RequirePackage{ifthen}
\RequirePackage{tikz}
\RequirePackage{calc}

%% ========================================================================
%% ASSOCIATION DATA — Set these in your document preamble
%% ========================================================================

\newcommand{\pn@associationname}{PoliNetwork APS}
\newcommand{\pn@email}{info@polinetwork.org}
\newcommand{\pn@pec}{polinetwork@pec.it}
\newcommand{\pn@privacyemail}{privacy@polinetwork.org}
\newcommand{\pn@website}{polinetwork.org}
\newcommand{\pn@websiteurl}{https://polinetwork.org}
\newcommand{\pn@instagram}{@polinetwork}
\newcommand{\pn@instagramurl}{https://www.instagram.com/polinetwork_/}
\newcommand{\pn@telegrambot}{@PoliNetwork3bot}
\newcommand{\pn@telegramurl}{https://t.me/PoliNetwork3bot}
\newcommand{\pn@president}{}
\newcommand{\pn@logopath}{../assets/Logo}  % without extension, will try .pdf then .png
\newcommand{\pn@signaturepath}{}

% Document metadata
\newcommand{\pn@doctitle}{}
\newcommand{\pn@docsubtitle}{}
\newcommand{\pn@docdate}{\today}
\newcommand{\pn@doclabel}{}
\newcommand{\pn@docprotocol}{}
\newcommand{\pn@docsubject}{}
\newcommand{\pn@docrecipient}{}
\newcommand{\pn@docversion}{}

%% ========================================================================
%% USER-FACING SETTERS
%% ========================================================================

\newcommand{\pnAssociationName}[1]{\renewcommand{\pn@associationname}{#1}}
\newcommand{\pnEmail}[1]{\renewcommand{\pn@email}{#1}}
\newcommand{\pnPEC}[1]{\renewcommand{\pn@pec}{#1}}
\newcommand{\pnPrivacyEmail}[1]{\renewcommand{\pn@privacyemail}{#1}}
\newcommand{\pnWebsite}[1]{\renewcommand{\pn@website}{#1}}
\newcommand{\pnWebsiteUrl}[1]{\renewcommand{\pn@websiteurl}{#1}}
\newcommand{\pnInstagram}[1]{\renewcommand{\pn@instagram}{#1}}
\newcommand{\pnInstagramUrl}[1]{\renewcommand{\pn@instagramurl}{#1}}
\newcommand{\pnTelegramBot}[1]{\renewcommand{\pn@telegrambot}{#1}}
\newcommand{\pnTelegramUrl}[1]{\renewcommand{\pn@telegramurl}{#1}}
\newcommand{\pnPresident}[1]{\renewcommand{\pn@president}{#1}}
\newcommand{\pnLogo}[1]{\renewcommand{\pn@logopath}{#1}}
\newcommand{\pnSignature}[1]{\renewcommand{\pn@signaturepath}{#1}}

\newcommand{\pnTitle}[1]{\renewcommand{\pn@doctitle}{#1}}
\newcommand{\pnSubtitle}[1]{\renewcommand{\pn@docsubtitle}{#1}}
\newcommand{\pnDocLabel}[1]{\renewcommand{\pn@doclabel}{#1}}
\newcommand{\pnDate}[1]{\renewcommand{\pn@docdate}{#1}}
\newcommand{\pnProtocol}[1]{\renewcommand{\pn@docprotocol}{#1}}
\newcommand{\pnSubject}[1]{\renewcommand{\pn@docsubject}{#1}}
\newcommand{\pnRecipient}[1]{\renewcommand{\pn@docrecipient}{#1}}
\newcommand{\pnVersion}[1]{\renewcommand{\pn@docversion}{#1}}

%% ========================================================================
%% CONTACT LINK HELPERS
%% ========================================================================

\newcommand{\pnWebsiteLink}{%
  \href{\pn@websiteurl}{\textcolor{pn-primary}{\texttt{\pn@website}}}%
}

\newcommand{\pnInstagramLink}{%
  \href{\pn@instagramurl}{\textcolor{pn-primary}{\texttt{\pn@instagram}}}%
}

\newcommand{\pnTelegramLink}{%
  \href{\pn@telegramurl}{\textcolor{pn-primary}{\texttt{\pn@telegrambot}}}%
}

%% ========================================================================
%% MULTILINGUAL SUPPORT (IT / EN)
%% ========================================================================

\newcommand{\pn@lang}{it}
\newcommand{\pnLanguage}[1]{\renewcommand{\pn@lang}{#1}}

% Italian strings (default)
\newcommand{\pn@str@page}{Pagina}
\newcommand{\pn@str@of}{di}
\newcommand{\pn@str@subject}{Oggetto}
\newcommand{\pn@str@to}{Spett.le}
\newcommand{\pn@str@protocol}{Protocollo}
\newcommand{\pn@str@date}{Data}
\newcommand{\pn@str@version}{Versione}
\newcommand{\pn@str@president}{Il Presidente}
\newcommand{\pn@str@email}{Email}
\newcommand{\pn@str@website}{Sito web}
\newcommand{\pn@str@contents}{Indice}

\newcommand{\pnSetEnglish}{%
  \renewcommand{\pn@lang}{en}%
  \renewcommand{\pn@str@page}{Page}%
  \renewcommand{\pn@str@of}{of}%
  \renewcommand{\pn@str@subject}{Subject}%
  \renewcommand{\pn@str@to}{To}%
  \renewcommand{\pn@str@protocol}{Protocol}%
  \renewcommand{\pn@str@date}{Date}%
  \renewcommand{\pn@str@version}{Version}%
  \renewcommand{\pn@str@president}{The President}%
  \renewcommand{\pn@str@email}{Email}%
  \renewcommand{\pn@str@website}{Website}%
  \renewcommand{\pn@str@contents}{Contents}%
}

\newcommand{\pnSetItalian}{%
  \renewcommand{\pn@lang}{it}%
  \renewcommand{\pn@str@page}{Pagina}%
  \renewcommand{\pn@str@of}{di}%
  \renewcommand{\pn@str@subject}{Oggetto}%
  \renewcommand{\pn@str@to}{Spett.le}%
  \renewcommand{\pn@str@protocol}{Protocollo}%
  \renewcommand{\pn@str@date}{Data}%
  \renewcommand{\pn@str@version}{Versione}%
  \renewcommand{\pn@str@president}{Il Presidente}%
  \renewcommand{\pn@str@email}{Email}%
  \renewcommand{\pn@str@website}{Sito web}%
  \renewcommand{\pn@str@contents}{Indice}%
}

%% ========================================================================
%% HEADER / FOOTER GENERATION
%% ========================================================================

% Insert the PoliNetwork logo
\newcommand{\pnInsertLogo}[1][1.2cm]{%
  \IfFileExists{\pn@logopath.pdf}{%
    \includegraphics[height=#1]{\pn@logopath.pdf}%
  }{%
    \IfFileExists{\pn@logopath.png}{%
      \includegraphics[height=#1]{\pn@logopath.png}%
    }{%
      \IfFileExists{\pn@logopath}{%
        \includegraphics[height=#1]{\pn@logopath}%
      }{%
        \textcolor{pn-text-muted}{\textit{[Logo]}}%
      }%
    }%
  }%
}

% Wordmark: "PoliNetwork" in Poppins Medium with pn-primary color
\newcommand{\pnWordmark}[1][16pt]{%
  {\poppinsmedium\fontsize{#1}{#1}\selectfont\color{pn-primary}PoliNetwork}%
}

% Render a rounded metadata pill.
% Parameter: #1 = short label text shown inside the pill.
\newcommand{\pnMetaPill}[1]{%
  \tikz[baseline=(pill.base)]{
    \node[
      fill=pn-white,
      draw=pn-blue-200,
      rounded corners=12pt,
      inner xsep=12pt,
      inner ysep=5pt,
      line width=0.4pt
    ] (pill) {\footnotesize\labelfont\color{pn-primary}#1};
  }%
}

% Render a compact metadata link/card with a title and description.
% Parameters: #1 = title/emphasis text, #2 = supporting description text.
\newcommand{\pnMetaLink}[2]{%
  \tikz[baseline=(link.base)]{
    \node[
      fill=white,
      fill opacity=0.82,
      text opacity=1,
      draw=pn-blue-100,
      rounded corners=7pt,
      inner xsep=12pt,
      inner ysep=6pt,
      line width=0.45pt
    ] (link) {%
      {\footnotesize\labelfont\color{pn-primary!80!pn-text}\textbf{#1}}\hspace{0.45em}%
      {\footnotesize\dmsansfont\color{pn-primary}#2}%
    };
  }%
}

% Example usage:
% \pnMetaPill{Associazione studentesca}
% \pnMetaLink{Sito web}{polinetwork.org}

% Make a print-friendly document header focused on the document title.
\newcommand{\pnMakeHeader}{%
  \thispagestyle{firstpage}%
  \vspace*{-0.25cm}%
  \noindent
  \begin{minipage}{\textwidth}
    \ifthenelse{\equal{\pn@doclabel}{}}{}{%
      {\small\labelfont\color{pn-primary}\MakeUppercase{\pn@doclabel}}\par\vspace{6pt}%
    }%
    \ifthenelse{\equal{\pn@doctitle}{}}{}{%
      {\fontsize{31pt}{36pt}\selectfont\headlinefont\bfseries\color{pn-primary}\pn@doctitle\par}%
    }%
    \ifthenelse{\equal{\pn@docsubtitle}{}}{}{%
      \vspace{7pt}%
      {\large\poppinsfont\fontseries{sb}\selectfont\color{pn-text}\pn@docsubtitle\par}%
    }%
  \end{minipage}
  \par\vspace{24pt}
}

\newcommand{\pnMakeMetadata}{%
  \ifthenelse{\equal{\pn@docsubject}{}\AND\equal{\pn@docrecipient}{}\AND\equal{\pn@docdate}{}\AND\equal{\pn@docversion}{}\AND\equal{\pn@docprotocol}{}}{}{%
    \vspace{2pt}%
    \noindent
    \begin{tabularx}{\textwidth}{@{}>{\labelfont\small\color{pn-primary}}p{3.2cm}X@{}}
      \ifthenelse{\equal{\pn@docdate}{}}{}{%
        \pn@str@date & {\color{pn-text}\pn@docdate}\\[5pt]%
      }%
      \ifthenelse{\equal{\pn@docversion}{}}{}{%
        \pn@str@version & {\color{pn-text}\pn@docversion}\\[5pt]%
      }%
      \ifthenelse{\equal{\pn@docprotocol}{}}{}{%
        \pn@str@protocol & {\color{pn-text}\pn@docprotocol}\\[5pt]%
      }%
      \ifthenelse{\equal{\pn@docrecipient}{}}{}{%
        \pn@str@to & {\color{pn-text}\pn@docrecipient}\\[5pt]%
      }%
      \ifthenelse{\equal{\pn@docsubject}{}}{}{%
        \pn@str@subject & {\color{pn-text}\pn@docsubject}\\[5pt]%
      }%
    \end{tabularx}
    \vspace{14pt}%
  }%
}

\newcommand{\pnMakeTitlePage}{%
  \thispagestyle{titlepage}%
  \vspace*{0.18\textheight}%
  \pnMakeHeader
  \pnMakeMetadata
  \thispagestyle{titlepage}%
  \vfill
  {\small\labelfont\color{pn-text-muted}\pn@associationname\par}%
  \clearpage
}

\newcommand{\pnClosing}[1][Cordiali saluti,]{%
  \vspace{1.5cm}
  \noindent #1
  
  \vspace{0.8cm}
  
  \noindent
  \begin{minipage}[t]{0.5\textwidth}
    \ifthenelse{\equal{\pn@signaturepath}{}}{%
      % No signature image: leave space for manual signature
      \vspace{1.5cm}
    }{%
      \includegraphics[height=1.5cm]{\pn@signaturepath}\\[2pt]
    }%
    \rule{5cm}{0.5pt}\\[2pt]
    {\small\color{pn-text-secondary}%
      \ifthenelse{\equal{\pn@president}{}}{%
        \pn@str@president\\
        \pn@associationname%
      }{%
        \pn@president\\
        \pn@str@president\\
        \pn@associationname%
      }%
    }
  \end{minipage}
}

%% ========================================================================
%% STYLED SECTION COMMANDS
%% ========================================================================

% Primary section heading, kept for backward compatibility with older sources.
\newcommand{\pnSection}[1]{%
  \vspace{30pt plus 4pt minus 2pt}%
  \noindent%
  {\Large\color{pn-primary}\headlinefont\textbf{#1}}%
  \par\vspace{12pt}%
}

% Subsection
\newcommand{\pnSubsection}[1]{%
  \vspace{18pt plus 2pt minus 1pt}%
  \noindent%
  {\large\color{pn-text}\headlinefont\fontseries{sb}\selectfont #1}%
  \par\vspace{8pt}%
}

% Compact heading for reusable content groups such as team names.
\newcommand{\pnTeamHeading}[1]{%
  \noindent%
  {\large\headlinefont\bfseries\selectfont\color{pn-primary}#1}%
  \par\vspace{3pt}%
}

%% ========================================================================
%% HIGHLIGHT BOX (for important notes)
%% ========================================================================

\newcommand{\pnHighlightBox}[1]{%
  \vspace{8pt}
  \noindent
  \begin{tikzpicture}
    \node[
      draw=pn-blue-100,
      fill=white,
      fill opacity=0.78,
      text opacity=1,
      rounded corners=12pt,
      line width=0.4pt,
      inner xsep=16pt,
      inner ysep=13pt,
      text width=\dimexpr\textwidth-34pt\relax,
      align=left
    ] (box) {%
      \color{pn-text}#1%
    };
    \fill[rounded corners=6pt, pn-primary]
      ($(box.north west)+(0.24,-0.26)$) rectangle ($(box.south west)+(0.08,0.26)$);
  \end{tikzpicture}
  \vspace{8pt}
}

% Inline highlight
\newcommand{\pnHighlight}[1]{%
  \textcolor{pn-primary}{\textbf{#1}}%
}

% Render a lead paragraph in the package's secondary text style.
% Parameter: #1 = introductory or summary text shown below a heading.
\newcommand{\pnLead}[1]{%
  {\large\color{pn-text-secondary}\dmsansfont #1\par}%
}

% Render an information card with a highlighted title and body copy.
% Parameters: #1 = card title, #2 = descriptive body text.
\newcommand{\pnInfoCard}[2]{%
  \begin{tikzpicture}
    \node[
      draw=pn-slate-200,
      fill=pn-white,
      rounded corners=12pt,
      line width=0.5pt,
      inner xsep=14pt,
      inner ysep=12pt,
      text width=\dimexpr\linewidth-34pt\relax,
      align=left
    ] {%
      {\large\headlinefont\color{pn-primary}\textbf{#1}}\par
      \vspace{6pt}
      {\color{pn-text}#2}
    };
  \end{tikzpicture}%
}

% Example usage:
% \pnLead{PoliNetwork promuove iniziative studentesche, tecnologiche e culturali.}
% \pnInfoCard{Contatti}{Scrivici a info@polinetwork.org per collaborazioni e supporto.}

%% ========================================================================
%% LEGACY SPACER
%% ========================================================================

\newcommand{\pnRule}{%
  \par\vspace{10pt}
}

%% ========================================================================
%% ARTICLE / CLAUSE NUMBERING (for legal documents)
%% ========================================================================

\newcounter{pn@article}
\newcounter{pn@clause}[pn@article]

\newcommand{\pnArticle}[1]{%
  \stepcounter{pn@article}%
  \vspace{22pt plus 4pt minus 2pt}%
  \noindent%
  {\Large\color{pn-primary}\headlinefont\textbf{Art.\,\thepn@article\hspace{0.6em}#1}}%
  \par\vspace{9pt}%
}

\newcommand{\pnClause}[1]{%
  \stepcounter{pn@clause}%
  \vspace{6pt}%
  \noindent%
  {\color{pn-text}\thepn@article.\thepn@clause}\hspace{6pt}%
  #1%
  \par\vspace{3pt}%
}

%% ========================================================================
%% NUMBERED SECTIONS (for privacy policies etc.)
%% ========================================================================

\newcounter{pn@numsec}

\newcommand{\pnNumberedSection}[1]{%
  \stepcounter{pn@numsec}%
  \vspace{22pt plus 4pt minus 2pt}%
  \noindent%
  {\Large\color{pn-primary}\headlinefont\textbf{\thepn@numsec.\hspace{0.6em}#1}}%
  \par\vspace{9pt}%
}

%% ========================================================================
%% CONTACT INFO BLOCK (for footer or end of document)
%% ========================================================================

\newcommand{\pnContactBlock}{%
  \vspace{16pt}
  \begin{center}
    {\small\color{pn-text-secondary}%
      \pn@associationname\quad%
      \pn@str@email: \pn@email\quad%
      PEC: \pn@pec\quad%
      \pn@str@website: \pn@website%
    }
  \end{center}
}

\endinput
